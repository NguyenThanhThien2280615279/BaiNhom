@model NotificationsViewModel
@using EcommerceSecondHand.Models
@{
    ViewData["Title"] = "Thông báo của tôi";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center">
        <h1>Thông báo của tôi</h1>
        <div>
            <form method="post" asp-controller="Notifications" asp-action="MarkAllAsRead">
                <button type="submit" class="btn btn-sm btn-outline-primary me-2">Đánh dấu tất cả là đã đọc</button>
            </form>
        </div>
    </div>
    
    <div class="row mt-4">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-body">
                    @if (Model.Notifications.Any())
                    {
                        <div class="list-group">
                            @foreach (var notification in Model.Notifications)
                            {
                                <div class="list-group-item list-group-item-action @(!notification.IsRead ? "bg-light" : "")">
                                    <div class="d-flex w-100 justify-content-between">
                                        <h5 class="mb-1">
                                            @{
                                                string iconClass = notification.Type switch
                                                {
                                                    NotificationType.System => "bi-info-circle",
                                                    NotificationType.Order => "bi-box",
                                                    NotificationType.Payment => "bi-credit-card",
                                                    NotificationType.Chat => "bi-chat-dots",
                                                    NotificationType.Product => "bi-tag",
                                                    NotificationType.Review => "bi-star",
                                                    _ => "bi-bell"
                                                };
                                            }
                                            <i class="bi @iconClass me-2"></i>
                                            @notification.Title
                                        </h5>
                                        <small class="text-muted">@notification.CreatedAt.ToString("g")</small>
                                    </div>
                                    <p class="mb-2">@notification.Message</p>
                                    <div class="d-flex justify-content-between">
                                        <div>
                                            @if (!string.IsNullOrEmpty(notification.ActionUrl))
                                            {
                                                <a href="@notification.ActionUrl" class="btn btn-sm btn-outline-primary">Xem chi tiết</a>
                                            }
                                        </div>
                                        <div>
                                            @if (!notification.IsRead)
                                            {
                                                <form method="post" asp-controller="Notifications" asp-action="MarkAsRead" class="d-inline">
                                                    <input type="hidden" name="notificationId" value="@notification.Id" />
                                                    <button type="submit" class="btn btn-sm btn-link">Đánh dấu đã đọc</button>
                                                </form>
                                            }
                                            <form method="post" asp-controller="Notifications" asp-action="Delete" class="d-inline">
                                                <input type="hidden" name="notificationId" value="@notification.Id" />
                                                <button type="submit" class="btn btn-sm btn-link text-danger">Xóa</button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                        
                        @if (Model.TotalPages > 1)
                        {
                            <div class="d-flex justify-content-center mt-4">
                                <nav>
                                    <ul class="pagination">
                                        @for (int i = 1; i <= Model.TotalPages; i++)
                                        {
                                            <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                                                <a class="page-link" asp-controller="Notifications" asp-action="Index" asp-route-page="@i">@i</a>
                                            </li>
                                        }
                                    </ul>
                                </nav>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="text-center py-5">
                            <p class="text-muted">Không tìm thấy thông báo nào.</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>